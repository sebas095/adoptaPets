<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Adopta Pets</title>
    <link href="https://fonts.googleapis.com/css?family=Roboto" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.98.2/css/materialize.min.css">
    <link href="https://fonts.googleapis.com/icon?family=Material+Icons" rel="stylesheet">
    <link href="https://file.myfontastic.com/e5H9LUj5bTPs52rQ4f96C6/icons.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/ghpages-materialize.css">
    <link rel="stylesheet" href="/css/adoptapets.css">
    <link rel="stylesheet" href="/css/style.css">
  </head>
  <body>
    <header>
      <% include ../menu.ejs %>
    </header>
    <main>
      <% include ../navbar %>
      <div class="row">
        <div class="col s12 m9 l10" style="padding: 0">
          <% include ../header %>
          <div class="container">
            <div id="session">
              <div class="scrollspy" id="post">
                <div class="row center">
                  <h3 class="light header icon ap-cat-3"> Publicación</h3>
                  <p>Publica una mascota en adopción.</p>
                </div>
              </div>
              <div class="divider"></div>
              <div class="row">
                <form action="/publications/<%= publication._id %>/edit?_method=put" id="publication-form" enctype="multipart/form-data" method="post">
                  <div class="scrollspy" id="pet">
                    <div class="row center">
                      <h4 class="light header icon ap-dog-8"> Información de la Mascota</h4>
                    </div>
                    <% if(publication.photos.length > 0) { %>
                      <div class="row">
                        <div class="slider">
                          <ul class="slides">
                            <% for (let i = 0; i < publication.photos.length; i++) { %>
                              <li><img src="/uploads/<%= publication.photos[i] %>"></li>
                            <% } %>
                          </ul>
                        </div>
                      </div>
                    <% } %>
                    <div class="row">
                      <div class="input-field col m6 s12">
                        <i class="material-icons prefix">color_lens</i>
                        <input type="text" id="pet.color" name="pet.color" class="validate" autocomplete="off" required="" disabled="" value="<%= publication.features.color %>" aria-required="true">
                        <label for="pet.color">Color</label>
                      </div>
                      <div class="input-field col m6 s12">
                        <i class="material-icons prefix">zoom_out_map</i>
                        <input type="text" id="pet.size" name="pet.size" class="validate" autocomplete="off" required="" disabled="" value="<%= publication.features.size %>" aria-required="true">
                        <label for="pet.size">Tamaño</label>
                      </div>
                    </div>
                    <div class="row">
                      <div class="input-field col m6 s12">
                        <i class="icon ap-dog-3 prefix"></i>
                        <input type="text" id="pet.name" name="pet.name" class="validate" autocomplete="off" required="" disabled="" value="<%= publication.features.name %>" aria-required="true">
                        <label for="pet.name">Nombre</label>
                      </div>
                      <div class="input-field col m6 s12">
                        <i class="material-icons prefix">add_circle</i>
                        <input type="text" id="pet.age" name="pet.age" class="validate" autocomplete="off" required="" disabled="" value="<%= publication.features.age %>" aria-required="true">
                        <label for="pet.age">Edad</label>
                      </div>
                    </div>
                    <div class="row">
                      <div class="input-field col m6 s12">
                        <i class="material-icons prefix">pets</i>
                        <select name="pet.gender" id="pet.gender" class="validate" required="" disabled="">
                          <option value="" disabled selected>Sexo</option>
                          <option value="m" <% if (publication.features.gender === 'm') { %> selected <% } %> >Macho</option>
                          <option value="f" <% if (publication.features.gender === 'f') { %> selected <% } %> >Hembra</option>
                        </select>
                      </div>
                      <div class="input-field col m6 s12 file-field">
                        <div class="btn">
                          <span>
                            ARCHIVO(S)
                          </span>
                          <input type="file" name="pet.photos" class="validate" accept='image/*' multiple disabled="" required="">
                        </div>
                        <div class="file-path-wrapper">
                          <input class="file-path validate" type="text" placeholder="Sube una o más imágenes">
                        </div>
                      </div>
                    </div>
                    <div class="row">
                      <div class="input-field col m12 s12">
                        <i class="material-icons prefix">location_on</i>
                        <!--<input type="text" id="address" name="address" class="validate" autocomplete="off" required="" aria-required="true">-->
                        <label>Ubicacion</label>
                        <input type="hidden" name="address" value="<%= publication.address %>" id="address">
                        <input type="hidden" name="lat" value="<%= publication.lat %>" id="lat">
                        <input type="hidden" name="lng" value="<%= publication.lng %>" id="lng">
                      </div>
                    </div>
                    <div class="row">
                      <div class="col s12 m12">
                        <br>
                        <div id="required-map" style="display: none" class="red">
                          <a class="close" onclick="hideAlert()" aria-label="close">&times;</a>
                          <p></p><strong>Error!</strong> Debe establecer una ubicacion para el cliente antes de registrarlo. Búsque la ubicación en el mapa y haga click alli.
                        </div>
                      </div>
                    </div>
                    <div class="row center">
                      <div class="input-field col m12 s12">
                        <div id="map" style="max-width: 100%"></div>
                      </div>
                    </div>
                  </div>
                  <div class="divider"></div>
                  <div class="scrollspy" id="contact">
                    <div class="row center">
                      <h4 class="light header icon ap-cat-8"> Información de Contacto</h4>
                    </div>
                    <div class="row">
                      <div class="input-field col m6 s12">
                        <i class="material-icons prefix">phone</i>
                        <input type="text" id="phone" name="phone" class="validate" autocomplete="off" required="" disabled="" value="<%= publication.phone %>" aria-required="true">
                        <label for="phone">Teléfono</label>
                      </div>
                      <div class="input-field col m6 s12">
                        <i class="material-icons prefix">email</i>
                        <input type="text" id="email" name="email" class="validate" autocomplete="off" required="" disabled="" value="<%= publication.email %>" aria-required="true">
                        <label for="email">E-mail</label>
                      </div>
                    </div>
                    <div class="row">
                      <div class="input-field col m12 s12">
                        <i class="material-icons prefix">assignment</i>
                        <textarea id="description" name="description" class="materialize-textarea validate" autocomplete="off" required="" disabled="" aria-required="true"><%= publication.description %></textarea>
                        <label for="description">Descripción</label>
                      </div>
                    </div>
                  </div>
                  <div class="row center">
                    <div class="col s12">
                      <button class="btn waves-effect waves-light blue" id="edit">Editar</button>
                      <button class="btn waves-effect waves-light green" id="save">Enviar</button>
                      <button class="btn waves-effect waves-light red" id="delete">Borrar</button>
                    </div>
                  </div>
                </form>
                <form class="" action="/publications/<%= publication._id %>?_method=delete" id="deletePublicationForm" method="post">
                </form>
              </div>
            </div>
          </div>
        </div>
        <div class="col hide-on-small-only m3 l2">
          <div class="toc-wrapper pinned">
            <ul class="section table-of-contents">
              <li><a href="#post">Publicación</a></li>
              <li><a href="#pet">Información Mascota</a></li>
              <li><a href="#contact">Información Contacto</a></li>
            </ul>
          </div>
        </div>
      </div>
    </main>
    <% include ../footer %>
    <script src="https://code.jquery.com/jquery-3.2.1.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/materialize/0.98.2/js/materialize.min.js"></script>
    <script src="https://maps.googleapis.com/maps/api/js?key=AIzaSyD7NUMl0ZdbpgytjC5N3nwIps43cXbeYpU"></script>
    <script src="/js/geolocation.js"></script>
    <script src="/js/editPublication.js"></script>
    <script type="text/javascript">
      (function($) {
        $(function() {
          $('.button-collapse').sideNav();
          $('.parallax').parallax();
          $('.scrollspy').scrollSpy();
          $('select').material_select();
          $('.slider').slider();
        }); // end of document ready
      })(jQuery); // end of jQuery name space
    </script>
  </body>
</html>
